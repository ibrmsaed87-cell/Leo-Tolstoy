# دليل حل المشاكل (Troubleshooting Guide)

## مشكلة: الملف غير موجود في التطبيق (File not found)

### الأعراض:
- رسالة خطأ: "الملف غير موجود في التطبيق" أو "File not found"
- المسار يظهر بشكل صحيح في الكود لكن الملف لا يُحمّل

### الحل:
1. **إعادة بناء APK بالكامل:**
   ```bash
   flutter clean
   flutter pub get
   flutter build apk --release
   ```

2. **التحقق من pubspec.yaml:**
   - تأكد من وجود `assets/books/` في قسم assets
   - تأكد من وجود المجلدات الفرعية: `assets/books/ar/`, `assets/books/en/`, `assets/books/ru/`

3. **التحقق من الملفات:**
   - تأكد من وجود الملفات فعلياً في المجلدات
   - تأكد من تطابق الأسماء (case-sensitive)

4. **إعادة تثبيت التطبيق:**
   - احذف التطبيق من الجهاز
   - ثبّت APK الجديد

### السبب:
- عند تحديث `pubspec.yaml` أو إضافة ملفات assets جديدة، يجب إعادة بناء التطبيق بالكامل
- APK القديم قد لا يحتوي على الملفات الجديدة
- `flutter clean` يزيل ملفات البناء القديمة ويضمن بناء جديد كامل

---

## مشكلة: عدم تحميل الإعلانات مع الإنترنت البطيء

### الأعراض:
- الإعلانات لا تظهر
- الإنترنت بطيء أو غير مستقر
- رسائل خطأ في console عن فشل تحميل الإعلانات

### الحل المطبق:

1. **زيادة وقت الانتظار للإعلانات:**
   - App Open Ad: إعادة محاولة متدرجة (5s, 10s, 15s, 20s, 30s)
   - Interstitial Ad: زيادة delay من 3s إلى 10s للأخطاء الشبكية
   - زيادة timeout من 5s إلى 15s في `_waitAndShowInterstitialAd`

2. **إضافة Exponential Backoff:**
   - إعادة المحاولة بفترات متزايدة تدريجياً
   - حد أقصى 5 محاولات للإعلانات المهمة

3. **تحسين معالجة الأخطاء:**
   - كشف أخطاء الشبكة تلقائياً
   - استخدام delay أطول للأخطاء الشبكية
   - سجلات تشخيصية مفصلة

4. **سجلات تشخيصية:**
   - `[AD LOADING]` لتتبع حالة تحميل الإعلانات
   - معلومات عن نوع الخطأ (network, timeout, etc.)
   - عدد المحاولات والوقت المتبقي

### الكود المحدث:
- `lib/utils/ad_helper.dart` - تحسين معالجة الأخطاء
- `lib/main.dart` - إضافة exponential backoff لـ App Open Ad
- `lib/screens/home_screen.dart` - زيادة delay للأخطاء الشبكية
- `lib/screens/reader_screen.dart` - زيادة timeout وdelay

### ملاحظات:
- الإعلانات تحتاج اتصال بالإنترنت
- مع الإنترنت البطيء، قد تستغرق الإعلانات وقتاً أطول للتحميل
- التطبيق سيستمر في المحاولة حتى 5 مرات
- بعد 5 محاولات فاشلة، سيتم إيقاف المحاولات لتوفير البطارية

